services:
  - type: web
    name: medical-studycat
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: APP_ADMIN_TOKEN
        sync: false
